\documentclass{article} 
\usepackage[final, nonatbib]{neurips_2025}

% --- Essential Packages ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}    % Good practice for NeurIPS
\usepackage[english]{}
\usepackage{hyperref}       % Hyperlinks
\usepackage{url}            % Simple URL typesetting
\usepackage{booktabs}       % Professional tables
\usepackage{amsfonts}       % Blackboard math symbols
\usepackage{nicefrac}       % Compact fractions
\usepackage{microtype}      % Microtypography
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{enumitem}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{siunitx}
\usepackage{pgfplots}
\usepackage{subcaption}
\pgfplotsset{compat=1.18}
\makeatletter
\providecommand{\@trackname}{} 
\makeatother

\usepackage{pgfplots}
\usepackage{tikz}
\definecolor{transcolor}{RGB}{31, 119, 180} % Blue
\definecolor{cnncolor}{RGB}{255, 127, 14}   % Orange
\definecolor{mlpcolor}{RGB}{44, 160, 44}    % Green

% --- Title Data ---
\title{\textbf{The Influence of Inductive Biases on Emergent Geometry under Tabula Rasa Constraints}}

\author{
    \textbf{Wang Yuancheng} \\
    Nanyang Technological University \\
    \textit{Undergraduate Research Experience on CAmpus (URECA)} \\
    \textit{Supervisor: A/P He Ying}
}
\date{\today}

\begin{document}

% --- Main Body ---
\maketitle
\begin{abstract}
Deep neural networks have become increasingly intelligent in recent years, yet, how they reason and represent complex environments remains unclear. Although recent work has shown that models can implicitly track game states in such domains, little work has been done to compare different foundational architectures under a strict \textit{tabula rasa} constraint. This raises a question: How differently do these networks reason and form internal representations when presented with minimal information? We investigate this by benchmarking Multi-Layer Perceptrons (MLPs), Convolutional Neural Networks (CNNs) and Transformers on the task of evaluating chess positions without human knowledge. When provided with only a numerical encoding of the current board state and a ground truth evaluation, we show that these networks pick up different nuances that fuel their evaluation due to inherent inductive biases. With various interpretability techniques, we study the differences in their learned representations to help explain network predictions.

\end{abstract}

% --- Section 1: Introduction ---
\section{Introduction}
Deep learning has been applied to a wide range of disciplines due to their advanced capabilities in solving complex tasks. However, the inner mechanics driving decisions that deep neural networks make are difficult to interpret, and has become an active field of research in recent years. Attempts in explainable artificial intelligence (XAI) in fields like natural language processing and computer vision have become increasingly successful \cite{alain2016understanding, belinkov2022probing}. Unfortunately, networks are typically trained on noisy, continuous and unstructured data, making them highly susceptible to shortcut learning \cite{geirhos2020shortcut}. These studies also regularly mix domain specific knowledge into foundational architectures. As a result, it is difficult to determine if these models are simply relying on surface patterns, or if their learned representations allow them to construct a \textit{world state} that they can reason with.

While prior work has been extensively done to study inherent inductive biases and reasoning capabilities of baseline architectures \cite{he2016deep, devlin2019bert, dosovitskiy2020image, tolstikhin2021mlp}, they typically rely on massive datasets like ImageNet or Wikipedia where a perfect state representation is impossible. In contrast, a board game like chess does provides a domain with perfect information and a set of strict rules that can serve as an ideal test environment \cite{toshniwal2022chess}. Moreover, the state space is vast, with number of possible states reaching upwards of $10^{46}$. Hence, we believe this is a great opportunity to observe how different architectures generalize and discover geometric patterns without human intervention.

This leads to our central question: are deep networks able to internalize the geometry and dynamics of chess without context or search? If so, how differently do they reason with the limited information provided to them? 

In this paper,  we look into 3 different model types: Multi-Layer Perceptrons (MLPs), Convolutional Neural Networks (CNNs) \cite{krizhevsky2012imagenet} and Transformers \cite{vaswani2017attention}. We investigate their differences in representation learning, specifically how different inherent inductive biases shape their learned representations of the board state under a strict \textit{tabula rasa} constraint. We show that models are able to form geometric relationships via supervised learning when provided only the current state and a ground truth position evaluation from Stockfish.

% --- Section 2: Related Work  ---
\section{Related Work}
To understand why we choose to investigate these 3 specific architectures, we have to look at the history of computer chess and the successful models that currently rule the playing field.

\subsection{Neural Chess Engines}
Initial breakthroughs in computer chess had shown that deep neural networks could replace manually tuned evaluation heuristics popular at that time\cite{lai2015giraffe, david2016deepchess}. Those early models utilized MLPs, which is remain as the backbone of Efficiently Updatable Neural Networks (NNUE) \cite{nasu2018efficiently} that powers state-of-the-art engines like Stockfish today. These engines leverage and improve on engineered input representations from previous generations such as piece to king relationships, along with advanced search algorithms to achieve superhuman play. Without these "add-ons", MLPs would see a pure 1D input vector and have to to learn chess concepts from scratch due to a lack of spatial information.

Later, CNNs emerged as a dominant architecture, as they are capable of capturing spatial relationships without the need for manual feature engineering \cite{silver2017alphazero}. AlphaZero, a chess engines arguably capable of defeating state-of-the-art engines back then, demonstrated that the usage of CNNs on chess evaluation functions can be very successful \cite{silver2017alphazero}. 

More recently, transformer architectures dominated the playing field, with open source implementations like Leela Chess Zero (Lc0) consistently winning 2nd place after Stockfish in online computer chess tournaments \cite{tcec_archive}. It was later shown that transformer based models can completely and efficiently internalize chess concepts, achieving grandmaster-level of play without any search algorithm or injection of domain specific knowledge \cite{ruoss2024grandmaster}.

\subsection{Representation Learning and Probing}
While frontier models achieve superhuman performance, their internal representations of the game state remain largely opaque. Researchers often use probing and dimensionality reduction techniques to analyze and interpret these black boxes\cite{alain2016understanding, jolliffe2016principal}. Other methods such as saliency maps \cite{simonyan2013deep}, Grad-CAM \cite{selvaraju2017grad} and attention weight visualizations \cite{clark2019does} have also proven to be useful.
These methods have been applied to perfect information games and uncovered human concepts and emergent internal representations of board state \cite{li2022emergent}\cite{mcgrath2022acquisition}. While these prior works show that models can internally reconstruct game geometry, there exists no rigorous comparative analysis of fundamental architectures under strict \textit{tabula rasa} constraints specifically in the domain of chess. 

% --- Section 3: Methodology  ---
\section{Methodology}
We now provide details on the methods for dataset curation, model training, model evaluation and result interpretability.

\subsection{Data} 
Our dataset consists of approximately 5.8M unique board states downloaded and extracted from CCRL website (\url{www.computerchess.org.uk/ccrl/}). Each state $s$ is run through Stockfish 17 at a depth of 20, which gives a centipawn score describing the position's evaluation $E(s)$. To mimic the intuition that a human grandmaster has when observing a position, we bin the centipawn scores $E(s)$ that yields a 7-class target $y(s) \in \{0, 1, \dots, 6\}$. We use four magnitude tiers: Equal ($|E(s)| < 100$), Better ($100 \le |E(s)| < 300$), Decisive Advantage ($300 \le |E(s)| < 500$) and Winning ($|E(s)| \ge 500$). Here, positive values indicate an advantage for White and negative values indicate an advantage for Black. 

% EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT
However, because chess evaluations are continuous, strict categorical boundaries introduces training instability (e.g., predicting the almost correct "Winning" label when $|E(s)| = 499$ ). Hence we map $E(s)$ to a continuous index and apply distance-aware Gaussian label smoothing to generate soft target distributions over the 7 classes (see Appendix \ref{label_smoothing}).
% EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT EDIT

In addition, we apply the following procedures to our raw dataset:

\textbf{Positional Perturbation:} When strong chess engines are paired against each other, positions that arise tend to be structured due to relatively optimal play. To ensure these networks are exposed to a wide variety of piece dynamics, we follow the methodology in \cite{lai2015giraffe} by applying one random legal move to each position in our dataset. This introduces the imbalanced and unstructured positions crucial for training a generalized evaluation function.

\textbf{Dataset Balancing:} Our initial exploratory data analysis revealed a heavy bias toward the "Equal" class. To prevent the trivial result of networks simply guessing the "Equal" class, we construct a perfectly uniform class distribution for both training and testing. We achieve this by under-sampling the "Equal" class and selectively augmenting minority classes via horizontal board mirroring.

\textbf{Filtering Volatile Positions:} Neural networks trained without lookahead algorithms act as static evaluation functions. When given a state with evaluations that shifts significantly at greater search depths, the ground truth depends more on deep branching calculations. To ensure the networks focus on internalizing spatial representations and immediate tactical patterns instead of struggling to approximate deep search trees, we remove the 0.53\% of positions that fulfills $|y_{20}(s) - y_{10}(s)| > 1$, where $y_d(s)$ denotes the assigned class label at Stockfish search depth $d$.

Unlike standard neural chess engines that normalize inputs to the perspective of White, we deliberately preserve the absolute, unrotated board state. Normalizing the perspective means removing knowledge of "Black's perspective" of the game. Not only does this violate our idea of \textit{tabula rasa} by using human knowledge of color and spatial symmetry, it misses the opportunity to observe if these networks can learn this symmetry independently. While we also utilize horizontal board mirroring to augment our dataset, that merely samples from other valid, reachable game states.

The dataset was split with a ratio of 80-10-10 for training, validation and testing respectively. It is worth noting that the split is not done randomly. In a game, the current position and the next would be likely have highly similar evaluations assuming ideal play. As a result, doing a random shuffle when creating the train-test-split introduces data leakage. This was shown clearly in our initial experiments, where a basic MLP achieved 87.66\% test accuracy when trained with shuffled data versus a much lower 54.37\% when trained with data split by games.

\subsection{Input Representation} 
We adopt a widely used multi-channel tensor for CNNs \cite{mcilroy2020aligning, silver2017alphazero}, and make some adjustments for MLPs and Transformers. Specifically, the input to each of the models are as follows:

\textbf{Spatial Feature Stack (CNNs):} The board state is represented as a stack of feature planes: $\mathbf{X}_{\text{stack}} \in \{0, 1\}^{19 \times 8 \times 8}$. The first 12 planes ($P_{1 \dots 12}$) represent the presence of each piece type respectively. Subsequently, the global state planes are added into the feature stack $\mathbf{X}_{stack} = \{ P_{1}, \dots, P_{12}, G, E \}$, where $G\in \{0, 1\}^{6 \times 8 \times 8}$ consist of constant planes filled with $1$s or $0$s indicating the side to move, castling rights and check status, and lastly, $E \in \{0, 1\}^{8 \times 8}$ is a one-hot encoding of the en-passant target square if any.

\textbf{Flattened Bitboards (MLPs):} MLPs lack spatial inductive biases and operate on 1D vectors. Hence, we flatten the board into a 775-dimensional binary vector $\mathbf{x}_{\text{flat}} \in \{0,1\}^{775}$ by concatenating the 12 piece bitboards ($12 \times 64 = 768$) with a 7-dimensional vector $\mathbf{g} \in \{0, 1\}^{7}$ encoding the same information present in the spatial feature stack.

\textbf{Spatial Tokenization (Transformers):} We deliberately avoid representing the board as a tokenized FEN string \cite{ruoss2024grandmaster} so that each individual square is explicitly tokenized. For instance, in the standard starting position (\texttt{rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1}), an entire rank of eight empty squares is compressed into a singular "8" token. This breaks the one-to-one mapping between sequence tokens and physical board coordinates, making it more difficult to observe any emergent geometry. Hence, we reshape the $19 \times 8 \times 8$ feature stack into a sequence of 64 tokens, where each token $\mathbf{t}_i \in \{0, 1\}^{19}$ represents the complete state of a single square.

In our input representation, we did not include the past 7 game states. This is because Chess can be modeled as an alternating Markov game, where decisions made by the active player depend only on the current game state \cite{littman1994markov, tang2024maia}. Furthermore, it was shown to provide marginal benefits for static evaluation \cite{tang2024maia}. On top of that, it gives us the chance to explore if the network can derive chess geometry regarding piece movements without prior knowledge of them.

\end{document}